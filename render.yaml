services:
  - type: web
    name: font-generator-backend
    env: python
    buildCommand: pip install -r requirements.txt && apt-get update && apt-get install -y potrace
    startCommand: python backend/server.py
    buildFilter:
      paths:
        - backend/**
        - requirements.txt
